import styled from 'styled-components';

export const AppContainer = styled.div`
  position: relative;
  min-height: 100vh;
  height: 100%;
  display: flex;
  flex-direction: column;
  margin: 0 4rem;
`;